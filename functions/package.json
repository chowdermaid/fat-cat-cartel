{
  "name": "fat-cat-cartel-functions",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "npm run build && node --test lib/authorization-policy.test.js",
    "register:discord": "node scripts/register-discord-commands.js"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@aws-sdk/client-ec2": "^3.1085.0",
    "@aws-sdk/client-ssm": "^3.1085.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": "22"
  }
}
